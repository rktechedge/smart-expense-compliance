[project]
name = "submission_frontend"
version = "0.1.0"
description = "Manager Dashboard service for ambient expense agent"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.28.0",
    "google-adk",
    "google-cloud-aiplatform",
]
